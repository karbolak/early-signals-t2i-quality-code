# Data and artifact notice

The MIT licence in this repository applies to the code only.

The final scored dataset under `artifacts/final_dataset/` is included for thesis documentation and reproducibility. It may contain prompts, generated-output metadata, automatic scorer outputs, and trajectory-derived features. These artifacts remain subject to the licences and terms of the underlying prompt dataset, image-generation model, scoring models, and computing environment.

Generated image folders, model weights, and cache directories are intentionally excluded.
